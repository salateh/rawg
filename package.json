{
  "name": "rawg",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && mkdir -p dist/css && cp src/project/css/style.css dist/css/"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
